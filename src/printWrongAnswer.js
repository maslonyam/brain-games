const printWrongAnswer = (answer, name, correctAnwser) => {
  console.log(`${answer} is wrong answer ;(. Correct answer was ${correctAnwser}.\nLet's try again, ${name}`);
};

export default printWrongAnswer;
